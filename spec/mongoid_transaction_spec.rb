require 'spec_helper'

describe MongoidTransaction do
  it 'has a version number' do
    expect(MongoidTransaction::VERSION).not_to be nil
  end

=begin
  it 'does something useful' do
    expect(false).to eq(true)
  end
=end
end
